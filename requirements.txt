# GSE SOP Quick-Finder Dependencies
# All dependencies run 100% locally - no cloud services
# Versions pinned for reproducibility in airgapped environments

# PDF Processing
pypdf==6.10.2
pdfminer.six==20231228

# Word Document Processing
python-docx==1.2.0

# Embeddings & Vector Search
sentence-transformers==5.4.1
faiss-cpu==1.9.0.post1

# Web Framework
Flask==3.1.3

# Data Processing
numpy==1.26.4
rank_bm25==0.2.2

# LLM for Answer Generation (pick one or both)
ollama==0.4.8

# Optional: Remote LLM providers (uncomment the one(s) you need)
# openai==1.82.0          # OpenAI, Azure, vLLM, LiteLLM
# anthropic==0.86.0       # Anthropic (Claude)

# Optional: Enhanced PDF viewing
# PyMuPDF==1.25.3
